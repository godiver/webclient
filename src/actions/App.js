import axios from 'axios'
export const GET_TITLE_EVENT = 'GET_TITLE_EVENT'

// const ROOT_URL = 

export const getTitleEvents = () => async dispatch => {
  const response = await axios.get(``)
  dispatch
}
